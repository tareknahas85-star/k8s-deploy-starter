apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-web
spec:
  tls:
    - hosts:
        - dev.demo.example.com
      secretName: demo-web-tls
  rules:
    - host: dev.demo.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dev-demo-web
                port:
                  name: http
