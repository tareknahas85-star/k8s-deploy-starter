apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-web-config
data:
  # Settings that are not secret.
  # Passwords and keys do NOT belong here. Use a Secret for those.
  APP_ENV: "base"
  LOG_LEVEL: "info"
